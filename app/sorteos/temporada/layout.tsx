// Layout sin Navbar normal — esta página se accede vía QR del local físico
// y debe ser full screen, sin distracciones.
export default function TemporadaLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
